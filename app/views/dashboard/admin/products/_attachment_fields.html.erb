<<<<<<< HEAD
<<<<<<< HEAD
<tr class="nested-fields justify-content-center">
    <td>
      <%= image_tag f.object.image.url, class: "rounded mx-auto d-block img-fluid mb-3" if f.object.image.url.present? %>
      <%= f.file_field :image, onchange: "readURL(this);" %>
      <%= f.hidden_field :image_cache, value: f.object.image_cache %>
    </td>
    <td class="thumbnail-fluid">
    <p class="thumb"></p>
      <%= f.label :description %>
      <%= f.text_field :description, :class => "form-control" %>
    </td>
    <td>
    </td>
    <td>
      <%= link_to_remove_association "移除這個檔案", f, :class => "btn btn-danger" unless f.object.new_record? %>
    </td>

  </div>
  <div class="mb-3">
    <%= f.file_field :image, onchange: "readURL(this);", class: "form-control img-upload" %>
    <%= f.hidden_field :image_cache, value: f.object.image_cache %>
  </div>
  <div class="mb-3">
        <div id="imgUpload">
      </div>
    <%= f.label "圖片描述", class: "form-label" %>
    <%= f.text_field :description, class: "form-control" %>
    <%= f.check_box :remove_image %>
  </div>
      <%= image_tag f.object.image.url, class: "rounded mx-auto d-block img-fluid product-img" if f.object.image.url.present? %>
  <div class="mb-3">
      <%= f.object.description %>
    <%= link_to_remove_association "移除這個檔案", f, class: "btn btn-danger" unless f.object.new_record? %>
  </div>
</div>
<script>
function readURL(input) {
  var files = document.querySelectorAll('img-upload')
  if (input.files && input.files[0]) {
      var reader = new FileReader();
        reader.onload = function (e) {
          var prevImg = document.getElementById('imgUpload');
          var result = e.target.result; //預覽圖片的data並塞一個element進去
          prevImg.innerHTML = '<img src="' + result + '" id ="prevImg" class= "rounded mx-auto d-block img-fluid" />';
        };
      reader.readAsDataURL(input.files[0]);
    }
      console.log(files.length)
  }
</script>
=======

<div class="nested-fields col-3" id="1-nested-fields">
  <div class="image-border">
    <div class="image-border-content">
      <div class="parent">
        <div class="image-content" id= "image-media-preview">
          <div class="font-awesome-image">
            <img id="<%= @attachments %>" alt="">
          </div>
        </div>
      </div>
      <label class="btn btn-light btn-sm" style="width: 100%;">
        Add a file!
        <span style="display:none;">
          <%= f.file_field :image, id: "image-input" %>
        </span>
      </label>
    </div>
  </div>
  <div class="links" style="margin-bottom: 10px;">
    <%= link_to_remove_association raw('<i class="far fa-trash-alt"></i> Remove'), f, class: "btn btn-light btn-sm", :style => "width: 100%; margin-top: -15px;" %>
  </div>
</div>


>>>>>>> react
